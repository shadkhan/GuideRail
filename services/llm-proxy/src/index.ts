// @guiderail/llm-proxy — STUB.
//
// The metered LLM pipe (auth, per-account caps, cache-by-content-hash, one
// outbound call) is spec 008. Runtime and framework are already decided in
// ADR-0006 (docs/adr/0006-llm-proxy-stack.md): TypeScript + Hono on Cloudflare
// Workers. This placeholder exists so the monorepo has the service the project
// map and spec 002 R1 require.

/** Placeholder — replaced by the real proxy in spec 008 (see ADR-0006). */
export const LLM_PROXY_STUB = true as const;
