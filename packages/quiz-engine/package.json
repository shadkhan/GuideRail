{
  "name": "@guiderail/quiz-engine",
  "private": true,
  "version": "0.0.0",
  "description": "GuideRail quiz + earned-time engine (spec 005).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo '@guiderail/quiz-engine is source-only (bundled by the extension)' && exit 0",
    "test": "vitest run",
    "lint": "eslint src",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
