The brand's primary action button — hard offset shadow instead of blur, lifts on hover, presses flat on click.
```jsx
<Button variant="primary" size="md">Start free trial</Button>
```
Variants: `primary` (margin-red fill), `secondary` (paper fill, ink border), `ghost` (no border/shadow, text only). Sizes: sm, md, lg. Supports `disabled`.
