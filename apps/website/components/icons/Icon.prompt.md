A small inline-SVG icon set (Lucide-style, 2px stroke, rounded caps) for cases where no product icon asset exists yet.
```jsx
<Icon name="shield" size={20} color="var(--ink-navy)" />
```
Variants: check, x, chevron-down, chevron-right, info, alert-triangle, shield, book-open, clock, lock.
